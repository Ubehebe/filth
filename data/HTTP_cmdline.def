/* See cmdline.hpp for details. Options without a short flag should
 * go at the bottom. */

DEFINE_ME_BOOL(ipv6, 6, ipv6, use IPv6, false)
DEFINE_ME_INT(cachesz, c, cache-mb, cache size (MB), 10)
DEFINE_ME_STR(default_resource, d, default-resource, resource to serve when "/" is requested, index.html)
DEFINE_ME_STR(sigdl_ext, e, sigdeadlock, emergency de-deadlock signal, USR1)
DEFINE_ME_STR(sigflush, f, sigflush, server flush signal, CONT)
DEFINE_ME_STR(ifnam, i, interface, network interface to use, lo)
DEFINE_ME_STR(mount, m, mount, where in the filesystem to mount, .)
DEFINE_ME_INT(port, p, port, listening port, 80)
DEFINE_ME_INT(listenq, q, listen-queue, TCP listen queue size, 10)
DEFINE_ME_INT(req_prealloc_sz, r, req-prealloc-mb, space to preallocate for HTTP requests (MB), 10)
DEFINE_ME_STR(sigdl_int, t, sigth, INTERNAL USE ONLY thread communication signal, USR2)
DEFINE_ME_INT(untrusted_uid, u, untrusted-uid, untrusted uid to execute as, 9999)
DEFINE_ME_INT(nworkers, w, num-workers, number of worker threads, 10)
DEFINE_ME_INT(cache_expires, x, cache-expires, default cache expiration time (minutes), 10)
DEFINE_ME_INT(max_req_body, -, max-req-body, maximum request body size (MB), 10)
DEFINE_ME_INT(max_req_hdr, -, max-req-header, maximum request header line length (bytes), 4096)
DEFINE_ME_INT(max_req_uri, -, max-req-uri, maximum request URI size (bytes), 4096)
DEFINE_ME_INT(tcp_keepalive_intvl, -, tcp-keepalive-intvl, advanced TCP option, 1)
DEFINE_ME_INT(tcp_keepalive_probes, -, tcp-keepalive-probes, advanced TCP option, 1)
DEFINE_ME_INT(tcp_keepalive_time, -, tcp-keepalive-time, advanced TCP option, 15)

