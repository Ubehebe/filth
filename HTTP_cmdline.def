/* Supported command-line options. Arguments:
 * 1: server's internal name for this option
 * 2: short (one-character) command-line flag
 * 3: long command-line flag
 * 4: description of the option, for use with --help
 * 5: option's default value
 * Should be in alphabetical order by argument 2 in order to look nice
 * for --help. */
DEFINE_ME_BOOL(ipv6, 6, ipv6, use IPv6, false)
DEFINE_ME_INT(cachesz, c, cache-size, static resource cache size (MB), 10)
DEFINE_ME_STR(default_resource, d, default-resource, resource to serve when "/" is requested, index.html)
DEFINE_ME_STR(ifnam, i, interface, network interface to use, lo)
DEFINE_ME_STR(mount, m, mount, where in the filesystem to mount, /)
DEFINE_ME_INT(port, p, port, listening port, 80)
DEFINE_ME_INT(nworkers, w, nworkers, number of worker threads, 10)
